# Changesets

Run `pnpm changeset` for every user-visible package change. Choose the smallest
SemVer bump that accurately describes the change and write release notes for
package consumers rather than an implementation log.
